/* eslint-disable */

enum Color {
    success = "success",
    danger = "danger",
    warning = "warning",
    default = "default",
    primary = "primary",
    secondary = "secondary",
}

enum Variant {
    bordered = "bordered",
    light = "light",
    flat = "flat",
}

export { Color, Variant };
